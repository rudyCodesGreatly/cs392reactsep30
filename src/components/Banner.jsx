import React from "react";


const Banner = ({title}) => (
    <div className="flex justify-center">
        <h1 className="bg-blue-200 p-5 mb-5 text-center font-semibold text-xl shadow-md inline-block">{title}</h1>
    </div>
    
)

export default Banner;
